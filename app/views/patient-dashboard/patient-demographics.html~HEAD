<div ng-if="personAttributes[0]">
   <h4><b>Person Attributes</b></h4>
     <div ng-repeat="attribute in personAttributes">
            <small>{{ attribute.name }}:{{ attribute.value }}</small>
      </div>
  </div>

  <div ng-if="patient.address().noAddress!='None'">
    <h5><b>Person Address</b></h5>
       <div ng-repeat="(key,value) in patient.address()">
         <div ng-if="value">
                  <small>{{key}} :  {{value}}</small>
          <div>
       </div>
    </div>
